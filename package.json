{
  "name": "todo-app-root",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "cd server && npm install && npm start",
    "heroku-postbuild": "cd client && npm install --only=dev --no-shrinkwrap && npm run build && cd .."
  },
  "author": "leanne.cr.do@gmail.com"
}
